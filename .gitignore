# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# 数据库运行时产物（种子与 DDL 已入库，devplan.db 作为初始状态随仓库分发）
db/*.bak
db/*.db-wal
db/*.db-shm
# 本地 QA 冒烟用的临时库副本
db/_smoke.db
# 重建前备份（backup_mysql.py 的产物，含业务数据，不入库）
db/_backup/

# 平台/会话文件
.codebuddy/
.workbuddy/
.DS_Store

# 本地环境变量（连接口令等，不入库）
.env
*.log
